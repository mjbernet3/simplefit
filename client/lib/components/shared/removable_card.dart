import 'package:flutter/material.dart';

// TODO: Create shared card that can be removed with different child content
class RemovableCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
